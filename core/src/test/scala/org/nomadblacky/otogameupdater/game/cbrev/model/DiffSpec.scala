package org.nomadblacky.otogameupdater.game.cbrev.model

import org.scalatest.{FunSpec, Matchers}

class DiffSpec extends FunSpec with Matchers {

  describe("Upper") {
    describe("#unapply") {
      it("should returns Some when `after` upper than `before`") {
        pending
      }

      it("should returns None when `after` lower than `before`") {
        pending
      }
    }
  }

  describe("Lower") {
    describe("#unapply") {
      it("should returns Some when `after` lower than `before`") {
        pending
      }

      it("should returns None when `after` upper than `before`") {
        pending
      }
    }
  }
}